// Q4. Write a ts program to check whether a number is divisible by 5 and 11 or not.
var num1 = 10;
var num2 = 2;
if (num1 > num2) {
    console.log(num1, "and", num2, "number is by divisible");
}
else {
    console.log(num1, "and", num2, "number is not by divisible");
}
